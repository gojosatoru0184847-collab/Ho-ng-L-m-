// Placeholder for a full Shader Graph (node-based). In a 12k-line build,
// this would include: node registry, codegen, uniform packing, preview modes.
export class VFXGraph {
  constructor(){ this.nodes=[]; }
  // TODO: addNode(type, params), compile(), bind(), etc.
}
